prompt = """
# 任务：生成java项目的测试用例
你是这个软件项目的架构师。扫描pom.xml和src目录下的java代码，生成测试用例

## 项目介绍
测试阶段，用的h2内存数据库
如果遇到需要数据库，可使用注解@DataJpaTest

## 执行步骤
忽略实体，和controller
生成每个用例需要检查是否正确，如参数。确保可编译通过。
每个文件生成后，调用根目录的mvnw 命令进行测试。测试通过再处理下一个文件

"""